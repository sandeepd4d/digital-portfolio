query HomeContent($id:ID!) {
	page(id:$id) {
        home {
            masterPieceDescription
            masterPieceTitle
            masterPieceImage {
                sourceUrl
            }
        }        
    }
}